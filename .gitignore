# Plugin build artifacts — regenerated by src/build.sh, not source.
src/*.so
src/*.o

# Output written by a real neoscrystallize run (see CONTEXT.md's
# verification pipeline) — a real result, not a fixture to keep.
result.txt

# Local, machine-specific Claude Code settings
.claude/settings.local.json
